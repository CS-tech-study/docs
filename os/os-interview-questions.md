# IPC (Inter Process Communication)

## 개요

### 정의

운영체제 내에서 서로 독립적인 프로세스들이 데이터를 주고받거나 자원을 공유할 수 있도록 하는 통신 메커니즘

### 사용 이유

일반적으로 각 프로세스는 고유의 주소 공간을 가지기 때문에 직접 메모리를 공유할 수 없습니다.

때문에 동기화, 자원 공유 등을 위해서는 IPC 기법을 이용해야합니다.

![image.png](/os/images/ipc.png)

## 기법 - 공유 메모리

### 공유 메모리

여러 프로세스가 동일한 메모리 영역을 공유하여 데이터를 직접 읽고 쓸 수 있도록 하는 기법입니다.

- 장점
    - 빠른 속도
    - 커널의 개입없이 메모리에 접근 가능합니다. → 적은 오버헤드
- 단점
    - 동시성 문제가 발생할 수 있습니다.
- 언제 사용?
    - 대량의 데이터를 빠르게 공유해야할 때 사용합니다.

### 메모리 맵 파일

파일의 내용을 메모리에 맵핑하여, 파일 I/O 대신 메모리 접근처럼 데이터를 처리할 수 있게 하는 기법입니다.

![image.png](/os/images/memorymap.png)

- 장점
    - 파일 IO 보다 빠른 메모리 IO로 접근 가능합니다.
- 단점
    - 메모리와 파일 간의 데이터 불일치 가능합니다.
- 언제 사용?
    - 파일로 대용량 데이터를 공유 할 때, FILE IO가 느릴 때  사용합니다.

## 기법 - 메시지 전달 (Message Passing)

### 파이프, 네임드 파이프

파이프는 한 프로세스의 출력 데이터를 다른 프로세스의 입력으로 전달하는 단방향 통신 메커니즘입니다.

네임드 파이프는 이름을 가지는 파이프로, 서로 다른 프로세스들이 PIPE의 이름만 알면 통신이 가능합니다.

- 장점
    - 네임드의 경우 서로 다른 프로세스 간 PIPE의 이름만 알면 통신이 가능합니다.
- 단점
    - 단방향 통신 → 쌍방 통신을 위해서는 Write용, Read용 PIPE 모두 필요합니다.
- 언제 사용?
    - 부모 자식 프로세스간 통신 할 때 사용합니다.

### Message Queue

메모리에서 동작하는 PIPE로 메시지 단위로 통신하는 특징이 있습니다.

각 메시지는 IFO(선입선출) 방식으로 저장되어 비동기적으로 전달됩니다.

![image.png](/os/images/messagequeue.png)

- 장점
    - 파이프와 달리 다수의 프로세스에게 메시지를 전달 가능합니다.
- 단점
    - 커널에서 제공하는 Message queue기 때문에 EnQueue에 제한이 존재합니다 
    = 커널 쓰레드에서만 가능 하다는 뜻?
- 언제 사용?
    - 채팅 서비스, 배치 처리, 분산 시스템 등에서 데이터를 비동기적으로 주고받을 때 사용합니다.

### Socket

네트워크 소켓통신을 시용하는 방식을 말합니다.

- 언제 사용?
    - 클라이언트-서버 모델 등 원격 통신을 할 때, 외부에 프로세스가 존재할 때 사용합니다.

### RPC (Remote Procedure Call)

클라이언트가 자신의 로컬 주소 공간에서 호출하는 것처럼, 원격 서버의 프로시저(함수나 메서드)를 실행할 수 있게 하는 분산 통신 기술입니다.

![image.png](/os/images/rpc.png)

- **장점**
    - IDL을 사용하여 서로 다른 플랫폼 간의 통신이 가능하게 합니다.
- 단점
    - 네트워크, 패킷 손실 등의 문제로 인해 실제로는  로컬 함수 호출보다 훨씬 느릴 수 있습니다.
    - 네트워크를 통한 통신이므로 인증, 암호화 등의 보안 메커니즘이 필수적입니다.
- 언제 사용?
    - MSA, 클라이언트-서버 모델, 원격 시스템 등에서 통신이 필요할 때 사용합니다.

---

### 관련 자료

https://dkswnkk.tistory.com/398

https://doitnow-man.tistory.com/110

---
---
# Redis Pub/Sub vs. Kafka

위에서 Message Queue 방식을 소개했는데 저희 스터디에서 자주 등장한 레디스와 카프카가 생각 났습니다.

그래서 레디스의 Pub/Sub과 카프카에 대해 비교해보고자 합니다.

## **기본 개념**

카프카는 게시자와 구독자가 공통 메시지 큐를 공유하여 구독자가 필요에 따라 메시지를 가져오는 풀 기반 시스템을 지원합니다. 

레디스는 이벤트 발생 시 게시자가 모든 구독자에게 메시지를 배포하는 푸시 기반 시스템을 지원합니다.
레디스는 이렇게 전달하기 때문에 메시지를 저장할 필요가 없고, 구독자가 없으면 메시지는 그저 사라지게 됩니다. 

| 비교 항목 | Redis Pub/Sub | Kafka |
| --- | --- | --- |
| 메시지 처리 방식 | **푸시(push) 방식** (구독자가 있어야 메시지가 전달) | **풀(pull) 방식** (소비자가 가져감) |
| 저장 여부 | **비저장** (구독한 클라이언트에게 즉시 전달) | **저장** (지정한 보관 기간 동안 유지 / RAM, 디스크) |
| 내구성 | **낮음** (구독자가 없으면 메시지 손실) | **높음** (구독자가 늦게 와도 메시지 읽기 가능) |

## 작동 방식

둘 간의 작동 방식에 차이가 있는 만큼 지원하는 기능도 다릅니다.

### **카프카 워크플로**

1. 생산자는 브로커에게 메시지를 게시합니다. 
2. 브로커는 메시지를 수신하면 데이터를 주제로 분류하고 데이터를 파티션에 저장합니다. 
3. 소비자는 관련 주제에 연결하여 해당 파티션에서 데이터를 추출합니다.

### **레디스 워크플로**

1. 생산자가 특정 노드에 메시지를 보냅니다.
2. 레디스에서 메시지 키를 확인하여 연결된 모든 가입자에게 메시지를 전달합니다. 
- 소비자는 메시지를 수신하기 위해 항상 레디스 서버와의 활성 연결을 시작하고 유지해야 합니다.

| 비교 항목 | Redis Pub/Sub | Kafka |
| --- | --- | --- |
| 구독 방식 | **브로드캐스트 방식** (구독한 모든 클라이언트에게 전송) | **컨슈머 그룹**을 통해 메시지를 분배 |
| 메시지 순서 보장 | **불가능** | **가능** (파티션 단위로 정렬됨) |
| 재시도 가능 여부 | **불가능** (실시간 처리만 가능) | **가능** (컨슈머가 읽을 위치를 조정 가능) |
| 병렬처리 | 불가능 | 여러 소비자가 동일한 메시지를 동시에 검색 가능  |
| 지연시간 | 매우 짧은 지연시간  |  데이터 복제로 인해 Redis보다 약간 느림 |

---

## **정**리

### Redis Pub/Sub

- 실시간성과 낮은 지연 시간이 중요한 애플리케이션에 적합합니다.
- 자주 액세스하는 데이터를 저장하거나 긴급 메시지를 전달하는 세션 캐시로 사용될 수 있습니다.

### Kafka

- 대량의 데이터를 안정적으로 저장하고, 여러 소비자가 처리할 때 적합합니다.
- 마이크로서비스 간의 실시간 통신, 이벤트 스트리밍, 로그 분석, 비동기 메시징과 같은 확장성이 중요한 작업에 사용될 수 있습니다.

---

### 관련 자료

https://aws.amazon.com/ko/compare/the-difference-between-kafka-and-redis/